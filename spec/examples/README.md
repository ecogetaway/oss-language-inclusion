# Example `i18n-signals.yml` files

These are **illustrative**. They are not scores and not a claim that the named
ecosystems have adopted the file.

| File | What it shows |
| --- | --- |
| [`oss-language-inclusion.i18n-signals.yml`](oss-language-inclusion.i18n-signals.yml) | This research repo, filled honestly (limited review capacity; lint exists; plural check does not) |
| [`python-gettext.i18n-signals.yml`](python-gettext.i18n-signals.yml) | A project that takes `.po` files and is open to new locales |
| [`js-json.i18n-signals.yml`](js-json.i18n-signals.yml) | A JS app with JSON locales, fixes-only |
| [`docs-only.i18n-signals.yml`](docs-only.i18n-signals.yml) | Documentation translations welcome; UI strings frozen |

Copy one, delete comments you do not need, and keep empty lists if that is the
truth.
